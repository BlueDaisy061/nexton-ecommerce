export type ProductDetail = {
    id: string,
    productName: string,
    price: number,
    salePrice: number,
    productCategory: string,
    image: string,
    date: Date,
    available: boolean
}